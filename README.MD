# booKeeper - version 0.0.3 
## Update - Flaming Hot Stuff

### Setup instructions:

- So, you want to open terminal in your computer as administrator and run this, just make sure to ad dpython to PATH during srtup:
```winget install --id Git.Git -e -; winget install --id Python.Python.3.13 -e --source winget```
\- This installs python and git k?

- Now you want to create an account at https://www.mongodb.com/products/platform and then copy the key in Drivers/Python iirc. Also make sure to replace the username with the one given at beginning

- Ok keep that string with you, or put it in notepad, DO NOT SHARE IT WITH ANYBODY ANYWHERE

-now run this command
```git clone https://github.com/hyp-n/booKeeper.git; cd booKeeper; .\build.bat```

- and then put the string in .env in that folder

- lowk this shd work


## TODO:
- [ ] Make an update script
- [ ] implement page counting and folder based management
